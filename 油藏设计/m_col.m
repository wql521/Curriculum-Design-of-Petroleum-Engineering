function y = m_col(x)
    % 函数 m_col 计算双指数曲线拟合的值
    % 输入:
    %   x - 自变量
    % 输出:
    %   y - 因变量值
    
    % 拟合参数
    a = 1.5226e+03;
    b = 0.0058;
    c = -667.6865;
    d = -1.9871;
    
    % 计算函数值
    y = a * exp(b * x) + c * exp(d * x);
end
